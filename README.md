# shelter-check-gym
Ongoing repository for work on agent-based modeling to find tax loopholes

note that clerk build must be run inside the repo of a folder to make the corresponding source code that is used in python. Also pathnames are very
sensitive and due to pathname resolution we name the repo where this source code is generated something slightly different each time.
